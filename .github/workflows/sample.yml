name: Sample actions

on:
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - users: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello World!
